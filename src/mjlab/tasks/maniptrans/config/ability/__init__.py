# ability hand not registered. Add BODY_MAPPING to
# asset_zoo/hands/ability/constants.py and `register_hand("ability", get_cfg,
# BODY_MAPPING)` here to enable. Robot's MJCF must define `right_palm` /
# `left_palm` sites at the canonical Z-up wrist frame.
